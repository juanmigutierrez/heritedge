# Modalities

P5 + P2 fill this in. Replace the stub rows.

An LLM is **not** a modality — it's the NLU/dialogue engine behind the voice and chat channels. List it separately after the table.

| Modality | Direction | Used in | Rationale |
|---|---|---|---|
| Voice (speech) | Input + output | Task 1, 2, 3 | Hands-free use outdoors; consistent with the differentiation angle (accessibility-first) |
| Touch / GUI | Input + output | Task 1, 2, 3 | Fallback for every voice action; primary in noisy environments |
| Camera / Vision | Input | Task 2, 3 | AR placement (WebXR Hit Test); artifact recognition in the treasure hunt |

## Processing engine (not a modality)

The conversational answers are generated by an LLM backed by a retrieval-augmented-generation pipeline over a curated Piazza Duomo knowledge base. It sits behind the voice and chat channels, not alongside them.
